services:
  java-maven-app:
    image: ${IMAGE_NAME}
    ports:
      - 3080:3080

  postgres:
    image: postgres:15
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: password
   
